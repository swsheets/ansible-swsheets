description "edgebuilder_primary"

start on runlevel [2345]
stop on runlevel [016]

respawn

env MIX_ENV=prod
env PORT={{ edgebuilder_primary_port }}
env HOME=/root
export MIX_ENV
export PORT
export HOME

chdir /srv/edgebuilder/current
exec /usr/local/bin/mix phoenix.server
